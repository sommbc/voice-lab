# Screenshots

No screenshots are checked in for the early release.

Add screenshots only when they are real captures of the current app. Do not add mockups, generated placeholders, private transcripts, private filenames, reference audio metadata, or generated run artifacts.

Recommended public screenshots after validation:

- main workflow with empty/sample-safe fields
- saved reference state without private filenames
- generation progress with non-private sample text
- finished MP3 state with a synthetic filename
